networks:
  acl-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.31.0.0/24

x-fips-common: &fips-common
  build:
    context: ../docker
  image: fips-test:latest
  entrypoint: ["/usr/local/bin/entrypoint.sh"]
  cap_add:
    - NET_ADMIN
  devices:
    - /dev/net/tun:/dev/net/tun
  sysctls:
    - net.ipv6.conf.all.disable_ipv6=0
  restart: "no"
  environment:
    - FIPS_TEST_MODE=default
    - RUST_LOG=info
  volumes:
    - ../docker/resolv.conf:/etc/resolv.conf:ro
    - ./acl/peers.allow:/etc/fips/peers.allow:ro
    - ./acl/peers.deny:/etc/fips/peers.deny:ro

services:
  node-a:
    <<: *fips-common
    container_name: fips-acl-a
    hostname: node-a
    volumes:
      - ../docker/resolv.conf:/etc/resolv.conf:ro
      - ./acl/peers.allow:/etc/fips/peers.allow:ro
      - ./acl/peers.deny:/etc/fips/peers.deny:ro
      - ./node-a/fips.yaml:/etc/fips/fips.yaml:ro
      - ./node-a/fips.key:/etc/fips/fips.key:ro
    networks:
      acl-net:
        ipv4_address: 172.31.0.10

  node-b:
    <<: *fips-common
    container_name: fips-acl-b
    hostname: node-b
    volumes:
      - ../docker/resolv.conf:/etc/resolv.conf:ro
      - ./acl/peers.allow:/etc/fips/peers.allow:ro
      - ./acl/peers.deny:/etc/fips/peers.deny:ro
      - ./node-b/fips.yaml:/etc/fips/fips.yaml:ro
      - ./node-b/fips.key:/etc/fips/fips.key:ro
    networks:
      acl-net:
        ipv4_address: 172.31.0.11

  node-c:
    <<: *fips-common
    container_name: fips-acl-c
    hostname: node-c
    volumes:
      - ../docker/resolv.conf:/etc/resolv.conf:ro
      - ./acl/peers.allow:/etc/fips/peers.allow:ro
      - ./acl/peers.deny:/etc/fips/peers.deny:ro
      - ./node-c/fips.yaml:/etc/fips/fips.yaml:ro
      - ./node-c/fips.key:/etc/fips/fips.key:ro
    networks:
      acl-net:
        ipv4_address: 172.31.0.12

  node-d:
    <<: *fips-common
    container_name: fips-acl-d
    hostname: node-d
    volumes:
      - ../docker/resolv.conf:/etc/resolv.conf:ro
      - ./acl/peers.allow:/etc/fips/peers.allow:ro
      - ./acl/peers.deny:/etc/fips/peers.deny:ro
      - ./node-d/fips.yaml:/etc/fips/fips.yaml:ro
      - ./node-d/fips.key:/etc/fips/fips.key:ro
    networks:
      acl-net:
        ipv4_address: 172.31.0.13
